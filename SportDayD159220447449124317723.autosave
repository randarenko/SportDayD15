Tower tower1;
Background ground;

void setup() {
  size(800, 600);
  //tower1 = (imgName, towerImgX, towerImgY)
  tower1 = new Tower("tower_pic.jpeg", width/2, height/2);
  ground = new Background("Ground.png", width/2, height*1.018);
}
void draw() {
  background(255);
  display();
}

void display() {
  tower1.display();
  ground.display();
}
